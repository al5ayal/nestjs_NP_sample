import { AuthExceptionsFilter } from './auth-exceptions.filter';

describe('AuthExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new AuthExceptionsFilter()).toBeDefined();
  });
});
